// The site's public address. Everything that needs an absolute URL (canonical
// links, social tags, structured data, sitemap, robots.txt) reads it from here,
// including index.html via the %SITE_URL% token (replaced at build time).
export const siteUrl = "https://www.bonevane.dev";
